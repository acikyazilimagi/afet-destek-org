name: deprem_destek
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core:
  firebase_auth:
  cloud_firestore:
  flutter_bloc:
  collection:

dev_dependencies:
  flutter_lints: 2.0.0
  very_good_analysis: 3.1.0
  freezed: 2.3.2
  build_runner: 2.3.3

flutter:
  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Medium.ttf
